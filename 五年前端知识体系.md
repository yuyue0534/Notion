下面这份可以当作 **“五年前端工程师核心知识体系总纲”** 来准备。五年前端不再只是会写页面、会调接口，而是要能解释原理、设计方案、排查复杂问题、做工程治理、理解业务系统架构。

# 五年前端工程师核心知识体系

## 一、JS 深度能力

五年前端的 JS 要从“会用语法”提升到“理解运行机制、能写底层工具、能分析性能和异常”。

### 1. 数据类型与类型判断

需要掌握：

```js
typeof
instanceof
Object.prototype.toString.call()
Array.isArray()
Number.isNaN()
```

重点理解：

```js
typeof null === 'object'
typeof function() {} === 'function'
NaN !== NaN
0.1 + 0.2 !== 0.3
```

面试常问：

```js
typeof null 为什么是 object？
instanceof 原理是什么？
如何准确判断数组、对象、日期、正则？
```

底层理解：

`typeof` 判断的是底层类型标签；
`instanceof` 判断的是构造函数的 `prototype` 是否出现在对象原型链上。

---

### 2. 原型与原型链

必须掌握：

```js
function Person() {}
const p = new Person()

p.__proto__ === Person.prototype
Person.prototype.constructor === Person
Person.__proto__ === Function.prototype
Object.__proto__ === Function.prototype
Function.__proto__ === Function.prototype
```

重点：

对象通过 `__proto__` 向上查找属性；
函数有 `prototype`，普通对象没有显式 `prototype`；
构造函数、实例、原型对象三者关系要非常清楚。

常问问题：

```js
说一下原型链
new 做了什么
如何实现 instanceof
如何实现继承
class 的本质是什么
```

`new` 的过程：

```js
function myNew(fn, ...args) {
  const obj = Object.create(fn.prototype)
  const result = fn.apply(obj, args)
  return result !== null && typeof result === 'object' ? result : obj
}
```

---

### 3. 作用域、闭包、执行上下文

需要掌握：

```js
全局作用域
函数作用域
块级作用域
词法作用域
作用域链
执行上下文
变量提升
暂时性死区
```

闭包本质：

函数可以访问其定义时所在的词法作用域，即使这个函数在外部执行。

典型应用：

```js
防抖
节流
缓存
模块私有变量
函数柯里化
React Hooks
```

风险：

闭包引用外部变量，可能导致内存无法释放。

经典题：

```js
for (var i = 0; i < 3; i++) {
  setTimeout(() => console.log(i), 1000)
}
// 3 3 3

for (let i = 0; i < 3; i++) {
  setTimeout(() => console.log(i), 1000)
}
// 0 1 2
```

---

### 4. this 指向

规则优先级：

```js
new 绑定 > 显式绑定 > 隐式绑定 > 默认绑定
```

需要掌握：

```js
obj.fn()
fn()
fn.call(obj)
fn.apply(obj)
fn.bind(obj)
new Fn()
箭头函数 this
```

箭头函数没有自己的 `this`，它的 `this` 来自定义时外层作用域。

高频题：

```js
var name = 'global'

const obj = {
  name: 'obj',
  fn() {
    return () => console.log(this.name)
  }
}

obj.fn()() // obj
```

---

### 5. 异步机制与事件循环

必须掌握：

```js
调用栈
宏任务
微任务
事件循环
Promise
async/await
MutationObserver
requestAnimationFrame
setTimeout
setInterval
MessageChannel
```

宏任务：

```js
script
setTimeout
setInterval
setImmediate
I/O
UI rendering
```

微任务：

```js
Promise.then
queueMicrotask
MutationObserver
process.nextTick // Node
```

经典顺序：

```js
console.log(1)

setTimeout(() => {
  console.log(2)
})

Promise.resolve().then(() => {
  console.log(3)
})

console.log(4)

// 1 4 3 2
```

`async/await` 本质是 Promise + Generator 思想的语法糖。

```js
async function fn() {
  console.log(1)
  await Promise.resolve()
  console.log(2)
}

fn()
console.log(3)

// 1 3 2
```

---

### 6. Promise 原理

需要掌握：

```js
Promise 状态
pending
fulfilled
rejected
then 链式调用
错误穿透
Promise.resolve
Promise.reject
Promise.all
Promise.race
Promise.allSettled
Promise.any
```

核心点：

Promise 状态一旦改变不可逆；
then 返回新的 Promise；
then 回调进入微任务队列。

需要能手写简化版 Promise，至少能讲清楚：

```js
状态管理
回调收集
异步执行
链式调用
resolvePromise 过程
```

---

### 7. 深拷贝与浅拷贝

浅拷贝：

```js
Object.assign()
展开运算符
Array.prototype.slice()
Array.prototype.concat()
```

深拷贝难点：

```js
循环引用
Date
RegExp
Map
Set
Symbol
函数
原型链
不可枚举属性
```

简单版：

```js
function deepClone(obj, map = new WeakMap()) {
  if (obj === null || typeof obj !== 'object') return obj

  if (map.has(obj)) return map.get(obj)

  const clone = Array.isArray(obj) ? [] : {}
  map.set(obj, clone)

  for (const key in obj) {
    clone[key] = deepClone(obj[key], map)
  }

  return clone
}
```

---

### 8. 防抖、节流、并发控制

防抖：一段时间内多次触发，只执行最后一次。

```js
function debounce(fn, delay) {
  let timer = null
  return function (...args) {
    clearTimeout(timer)
    timer = setTimeout(() => {
      fn.apply(this, args)
    }, delay)
  }
}
```

节流：一段时间内最多执行一次。

```js
function throttle(fn, delay) {
  let last = 0
  return function (...args) {
    const now = Date.now()
    if (now - last >= delay) {
      last = now
      fn.apply(this, args)
    }
  }
}
```

并发请求控制：

```js
async function limitRequest(tasks, limit) {
  const results = []
  const executing = []

  for (const task of tasks) {
    const p = Promise.resolve().then(() => task())
    results.push(p)

    if (tasks.length >= limit) {
      const e = p.then(() => executing.splice(executing.indexOf(e), 1))
      executing.push(e)

      if (executing.length >= limit) {
        await Promise.race(executing)
      }
    }
  }

  return Promise.all(results)
}
```

---

### 9. 模块化

需要掌握：

```js
CommonJS
AMD
CMD
UMD
ES Module
```

CommonJS：

```js
const a = require('./a')
module.exports = {}
```

特点：

```js
运行时加载
值拷贝
同步加载
Node 常用
```

ES Module：

```js
import a from './a.js'
export default {}
```

特点：

```js
编译时静态分析
实时绑定
支持 Tree Shaking
浏览器原生支持
```

高频问题：

```js
ESM 和 CommonJS 区别
为什么 ESM 有利于 Tree Shaking
循环依赖如何处理
Vite 为什么基于 ESM 更快
```

---

## 二、框架原理

五年前端不能只停留在“会写 React/Vue 组件”，而要理解框架为什么这样设计。

# React 核心体系

## 1. React 设计思想

React 的核心思想：

```js
UI = f(state)
```

也就是：页面是状态的映射。

核心关键词：

```js
组件化
声明式
不可变数据
单向数据流
虚拟 DOM
Fiber
Hooks
并发渲染
调度机制
```

---

## 2. 虚拟 DOM

虚拟 DOM 本质是 JS 对象，用来描述真实 DOM。

```js
const vnode = {
  type: 'div',
  props: {
    className: 'box',
    children: 'hello'
  }
}
```

价值：

```js
跨平台
声明式 UI
批量 diff
减少直接 DOM 操作
方便实现组件抽象
```

注意：

虚拟 DOM 不一定比原生 DOM 快，它的价值主要是让复杂 UI 更新变得可维护。

---

## 3. React Diff

核心策略：

```js
同层比较
不同类型节点直接替换
列表依赖 key
```

为什么 key 重要？

key 用来帮助 React 判断节点是否可以复用。

错误写法：

```jsx
list.map((item, index) => <div key={index}>{item.name}</div>)
```

如果列表会增删改排序，不建议用 index 作为 key。

---

## 4. Fiber 架构

React 早期递归更新不可中断，复杂页面可能阻塞主线程。

Fiber 解决的问题：

```js
任务切片
可中断渲染
优先级调度
并发能力
更细粒度更新
```

Fiber 节点可以理解为 React 自己维护的一种链表结构：

```js
child
sibling
return
```

React 渲染分两阶段：

```js
render 阶段：可中断，生成 Fiber 树
commit 阶段：不可中断，更新真实 DOM
```

---

## 5. Hooks 原理

常用 Hooks：

```js
useState
useEffect
useMemo
useCallback
useRef
useReducer
useContext
useLayoutEffect
```

Hooks 本质依赖调用顺序，所以不能写在条件语句里。

错误：

```jsx
if (visible) {
  const [a, setA] = useState(1)
}
```

正确：

```jsx
const [a, setA] = useState(1)
```

`useEffect` 与 `useLayoutEffect` 区别：

```js
useEffect：浏览器绘制之后异步执行
useLayoutEffect：DOM 更新之后、浏览器绘制之前同步执行
```

---

## 6. React 性能优化

常见手段：

```js
React.memo
useMemo
useCallback
useRef
懒加载
虚拟列表
状态下沉 / 状态提升
避免不必要的 Context 更新
合理拆分组件
避免匿名函数频繁传递
避免大对象直接作为 props
```

典型场景：

父组件更新导致子组件无意义更新。

```jsx
const Child = React.memo(function Child({ value }) {
  return <div>{value}</div>
})
```

但是要注意：
`React.memo` 不是万能优化，过度使用也有比较成本。

---

## 7. React 状态管理

需要掌握：

```js
useState
useReducer
Context
Redux
MobX
Zustand
Jotai
Recoil
React Query
SWR
```

状态分类：

```js
本地 UI 状态
跨组件共享状态
服务端缓存状态
URL 状态
表单状态
全局业务状态
```

五年前端要能说清楚：

```js
Redux 适合复杂业务状态
React Query 适合服务端数据缓存
Zustand 适合轻量全局状态
Context 不适合频繁变化的大状态
```

---

# Vue 核心体系

## 1. Vue 响应式原理

Vue 2：

```js
Object.defineProperty
```

缺点：

```js
无法直接监听新增属性
数组部分操作需要特殊处理
初始化递归劫持成本高
```

Vue 3：

```js
Proxy
Reflect
```

优势：

```js
可以监听新增属性
可以监听删除属性
可以代理数组、Map、Set
性能和能力更强
```

核心流程：

```js
reactive 创建代理对象
track 收集依赖
trigger 触发更新
effect 执行副作用函数
```

简化版：

```js
const bucket = new WeakMap()

function reactive(obj) {
  return new Proxy(obj, {
    get(target, key) {
      track(target, key)
      return target[key]
    },
    set(target, key, value) {
      target[key] = value
      trigger(target, key)
      return true
    }
  })
}
```

---

## 2. Vue 编译原理

Vue 模板会被编译成 render 函数。

流程：

```js
template
  ↓
parse 生成 AST
  ↓
transform 优化 AST
  ↓
generate 生成 render 函数
  ↓
执行 render 生成 vnode
```

Vue 3 优化：

```js
静态提升
Patch Flag
Block Tree
缓存事件处理函数
```

---

## 3. Vue Diff

Vue 3 使用快速 Diff 思想。

重点：

```js
头头比较
尾尾比较
头尾比较
尾头比较
最长递增子序列
```

最长递增子序列用于减少 DOM 移动次数。

---

## 4. Vue 组件通信

常见方式：

```js
props / emits
v-model
provide / inject
ref
event bus
Pinia / Vuex
slots
attrs
expose
```

五年前端需要能判断：

```js
父子通信用 props/emits
跨层级稳定依赖用 provide/inject
复杂业务状态用 Pinia
组件内容扩展用 slot
表单封装常用 v-model
```

---

## 5. Vue 性能优化

常见手段：

```js
v-if / v-show 合理使用
v-for 加 key
computed 缓存
watch 控制副作用
keep-alive
defineAsyncComponent
路由懒加载
组件拆分
虚拟列表
避免深层响应式
shallowRef / shallowReactive
markRaw
```

---

## 三、工程化能力

五年前端非常重要的一条线：从“写业务代码”升级为“建设项目”。

## 1. 项目架构设计

需要掌握：

```js
目录结构设计
模块拆分
组件分层
路由设计
权限设计
状态管理设计
请求层封装
错误处理
环境配置
构建配置
规范治理
```

常见目录：

```txt
src
 ├─ api
 ├─ assets
 ├─ components
 ├─ hooks / composables
 ├─ layouts
 ├─ router
 ├─ store
 ├─ styles
 ├─ utils
 ├─ views
 ├─ permissions
 └─ main.ts
```

---

## 2. TypeScript

必须掌握：

```ts
基本类型
interface
type
泛型
联合类型
交叉类型
类型守卫
条件类型
映射类型
工具类型
声明文件
模块声明
```

常用工具类型：

```ts
Partial<T>
Required<T>
Readonly<T>
Pick<T, K>
Omit<T, K>
Record<K, T>
ReturnType<T>
Parameters<T>
InstanceType<T>
```

高级能力：

```ts
泛型组件
接口类型约束
表单 schema 类型推导
接口返回值类型封装
权限码类型约束
路由 meta 类型增强
```

示例：

```ts
interface ApiResponse<T> {
  code: number
  message: string
  data: T
}

async function request<T>(url: string): Promise<ApiResponse<T>> {
  const res = await fetch(url)
  return res.json()
}
```

---

## 3. 请求封装

需要掌握：

```js
Axios 封装
fetch 封装
Token 注入
Token 刷新
错误码处理
重复请求取消
接口重试
loading 合并
文件上传
文件下载
超时处理
请求缓存
```

Token 刷新重点：

```js
access_token 过期
refresh_token 换新 token
队列缓存失败请求
刷新完成后重放请求
刷新失败跳登录
```

---

## 4. 权限系统

企业后台高频：

```js
登录权限
路由权限
菜单权限
按钮权限
接口权限
数据权限
角色权限
组织权限
```

常见方案：

```js
前端根据权限码动态渲染菜单
路由 meta 配置权限
按钮指令控制显示
接口由后端最终兜底
```

示例：

```ts
{
  path: '/user',
  component: UserPage,
  meta: {
    title: '用户管理',
    permission: 'user:list'
  }
}
```

按钮权限：

```vue
<el-button v-permission="'user:add'">新增</el-button>
```

注意：

前端权限只能控制体验，不能替代后端鉴权。

---

## 5. 动态表单

需要掌握：

```js
schema 配置
字段联动
动态显隐
异步数据源
表单校验
默认值
回显转换
提交转换
组件映射
复杂嵌套表单
```

典型 schema：

```ts
const schema = [
  {
    label: '用户名',
    field: 'username',
    component: 'Input',
    rules: [{ required: true, message: '请输入用户名' }]
  },
  {
    label: '角色',
    field: 'role',
    component: 'Select',
    options: []
  }
]
```

---

## 6. 通用表格

高频能力：

```js
查询表单
分页
排序
筛选
操作列
权限按钮
列配置
固定列
导出
批量操作
行内编辑
虚拟滚动
高度自适应
```

五年前端要能抽象：

```js
useTable
TableProvider
schema columns
request adapter
toolbar slot
action slot
```

---

## 7. 组件库与设计系统

需要掌握：

```js
组件封装规范
Props 设计
事件设计
插槽设计
受控 / 非受控
主题变量
Design Token
暗黑模式
国际化
无障碍
单元测试
文档站点
```

组件设计重点：

```js
稳定 API
合理默认值
支持扩展
避免业务强耦合
类型完善
文档清晰
```

---

## 四、构建工具链

## 1. Webpack

需要掌握：

```js
entry
output
loader
plugin
mode
devServer
resolve
optimization
sourceMap
HMR
代码分割
Tree Shaking
缓存
```

Loader 本质：转换文件内容。

Plugin 本质：参与构建生命周期，扩展构建能力。

常见 Loader：

```js
babel-loader
ts-loader
css-loader
style-loader
less-loader
sass-loader
postcss-loader
file-loader
url-loader
```

常见 Plugin：

```js
HtmlWebpackPlugin
MiniCssExtractPlugin
DefinePlugin
CopyWebpackPlugin
ESLintPlugin
CompressionPlugin
```

---

## 2. Vite

Vite 核心优势：

```js
开发环境基于原生 ESM
按需编译
启动快
HMR 快
生产环境使用 Rollup 打包
```

需要掌握：

```js
依赖预构建
esbuild
Rollup
插件机制
环境变量
代理配置
别名配置
构建优化
```

Vite 为什么快？

```js
Webpack 开发阶段需要先打包再启动
Vite 开发阶段利用浏览器原生 ESM，按需加载
```

---

## 3. Babel

Babel 作用：

```js
语法转换
Polyfill 注入
JSX 转换
TypeScript 转换
插件体系
```

流程：

```js
parse
transform
generate
```

也就是：

```js
代码 -> AST -> AST 转换 -> 新代码
```

常见配置：

```js
@babel/preset-env
@babel/preset-react
@babel/preset-typescript
core-js
```

---

## 4. ESLint / Prettier / Stylelint

工程规范必备：

```js
ESLint：代码质量
Prettier：代码格式
Stylelint：CSS 规范
Husky：Git hooks
lint-staged：提交前检查
commitlint：提交信息规范
```

典型流程：

```txt
git commit
  ↓
husky pre-commit
  ↓
lint-staged
  ↓
eslint / prettier
  ↓
commitlint
```

---

## 5. Monorepo

需要掌握：

```js
pnpm workspace
npm workspace
yarn workspace
Turborepo
Nx
Lerna
```

适合场景：

```js
组件库
工具库
多应用共享模块
微前端子应用
大型业务系统
```

常见结构：

```txt
packages
 ├─ ui
 ├─ utils
 ├─ hooks
 └─ config

apps
 ├─ admin
 └─ mobile
```

---

## 五、网络与浏览器

## 1. HTTP

必须掌握：

```js
HTTP/1.1
HTTP/2
HTTP/3
请求方法
状态码
请求头
响应头
缓存
Cookie
跨域
HTTPS
```

常见状态码：

```txt
200 成功
204 无内容
301 永久重定向
302 临时重定向
304 协商缓存命中
400 请求错误
401 未登录
403 无权限
404 不存在
500 服务端错误
502 网关错误
504 网关超时
```

---

## 2. HTTP 缓存

强缓存：

```txt
Cache-Control
Expires
```

协商缓存：

```txt
Last-Modified / If-Modified-Since
ETag / If-None-Match
```

优先级：

```txt
Cache-Control > Expires
ETag > Last-Modified
```

常见策略：

```txt
HTML 不强缓存
JS/CSS 文件名加 hash，长期强缓存
图片资源合理缓存
接口数据根据业务缓存
```

---

## 3. HTTPS

需要掌握：

```js
对称加密
非对称加密
数字证书
CA
TLS 握手
证书校验
会话密钥
```

简单理解：

```txt
非对称加密解决密钥交换
对称加密负责后续通信
数字证书防止中间人攻击
```

---

## 4. 跨域

浏览器同源策略限制：

```txt
协议、域名、端口必须一致
```

常见解决方案：

```js
CORS
JSONP
反向代理
postMessage
WebSocket
Nginx 代理
```

CORS 重点头：

```txt
Access-Control-Allow-Origin
Access-Control-Allow-Methods
Access-Control-Allow-Headers
Access-Control-Allow-Credentials
```

简单请求：

```txt
GET
POST
HEAD
部分 Content-Type
```

复杂请求会触发预检请求：

```txt
OPTIONS
```

---

## 5. 浏览器渲染机制

流程：

```txt
HTML -> DOM
CSS -> CSSOM
DOM + CSSOM -> Render Tree
Layout
Paint
Composite
```

需要区分：

```txt
重排 / 回流：布局变化
重绘：样式变化但不影响布局
合成：GPU 图层合成
```

触发重排：

```js
修改宽高
修改位置
读取 offsetTop / offsetWidth
修改字体
添加删除 DOM
```

优化：

```js
批量修改 DOM
使用 transform 替代 top/left
使用 opacity 替代 visibility 变化
避免频繁读取布局属性
requestAnimationFrame
虚拟列表
```

---

## 6. 浏览器存储

需要掌握：

```js
Cookie
localStorage
sessionStorage
IndexedDB
Cache Storage
Memory Cache
Service Worker
```

对比：

```txt
Cookie：自动随请求发送，容量小，适合认证相关
localStorage：持久化，容量较大，不随请求发送
sessionStorage：会话级别
IndexedDB：大量结构化数据
Cache Storage：PWA 离线缓存
```

---

## 六、安全

五年前端必须懂安全，尤其是企业项目。

## 1. XSS

XSS：恶意脚本注入。

类型：

```txt
存储型 XSS
反射型 XSS
DOM 型 XSS
```

防御：

```js
输入过滤
输出转义
避免 v-html / dangerouslySetInnerHTML
CSP
HttpOnly Cookie
富文本白名单
```

危险示例：

```js
element.innerHTML = userInput
```

---

## 2. CSRF

CSRF：攻击者诱导用户浏览器带着登录态发请求。

防御：

```js
SameSite Cookie
CSRF Token
Referer 校验
Origin 校验
验证码
关键操作二次确认
```

---

## 3. 点击劫持

攻击者用 iframe 嵌套页面，诱导用户点击。

防御：

```txt
X-Frame-Options: DENY
Content-Security-Policy: frame-ancestors 'none'
```

---

## 4. 接口安全

需要关注：

```js
鉴权
权限校验
签名
时间戳
防重放
限流
参数校验
敏感字段脱敏
日志审计
```

重点：

前端不要保存敏感密钥。
前端加密不能替代后端安全。
权限控制必须以后端为准。

---

## 5. npm 供应链安全

需要掌握：

```js
依赖漏洞
恶意包
拼写攻击
lockfile
npm audit
pnpm audit
依赖升级策略
私有源
CI 安全扫描
```

---

## 七、性能优化

性能优化要从“感知体验 + 加载速度 + 运行效率 + 构建产物”四个层面看。

## 1. 性能指标

核心指标：

```txt
FP：首次绘制
FCP：首次内容绘制
LCP：最大内容绘制
FID：首次输入延迟
INP：交互到下一次绘制
CLS：累计布局偏移
TTFB：首字节时间
TTI：可交互时间
```

重点关注：

```txt
LCP：首屏主体内容速度
INP：交互响应速度
CLS：页面稳定性
```

---

## 2. 加载性能优化

常见手段：

```js
代码分割
路由懒加载
组件懒加载
Tree Shaking
压缩 JS/CSS
图片压缩
WebP / AVIF
CDN
HTTP 缓存
预加载
预连接
DNS 预解析
服务端渲染
静态化
骨架屏
```

示例：

```js
const UserPage = React.lazy(() => import('./UserPage'))
```

Vue：

```js
const UserPage = () => import('@/views/UserPage.vue')
```

---

## 3. 运行时性能优化

常见手段：

```js
减少重排重绘
虚拟列表
Web Worker
requestAnimationFrame
requestIdleCallback
事件委托
防抖节流
缓存计算结果
避免大对象深度响应式
减少无意义渲染
```

大列表优化：

```js
虚拟滚动
分页
懒加载
增量渲染
```

---

## 4. React 性能排查

工具：

```js
React DevTools Profiler
why-did-you-render
Chrome Performance
Lighthouse
```

常见问题：

```js
父组件频繁更新
props 引用变化
Context 大范围更新
列表 key 不稳定
useEffect 依赖错误
重复请求
```

---

## 5. 构建产物优化

需要掌握：

```js
Bundle Analyzer
SplitChunks
Tree Shaking
动态 import
externals
CDN
gzip
brotli
source-map 策略
依赖替换
```

常见方案：

```js
moment 替换 dayjs
lodash 按需引入
echarts 按需引入
组件库按需加载
```

---

## 八、CSS 与样式工程化

五年前端的 CSS 不只是写样式，而是解决复杂布局、响应式、主题化和工程治理。

## 1. 布局能力

必须掌握：

```js
盒模型
BFC
Flex
Grid
定位
浮动
层叠上下文
z-index
响应式布局
移动端适配
```

Flex 高频：

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

`flex: 1` 本质：

```css
flex-grow: 1;
flex-shrink: 1;
flex-basis: 0%;
```

Grid 适合二维布局：

```css
.grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 16px;
}
```

---

## 2. BFC

BFC 触发方式：

```css
overflow: hidden;
display: flow-root;
display: flex;
display: grid;
position: absolute;
position: fixed;
float: left;
```

作用：

```txt
清除浮动
阻止 margin 合并
隔离布局
```

---

## 3. 移动端适配

需要掌握：

```css
rem
vw / vh
媒体查询
devicePixelRatio
1px 边框
safe-area
横竖屏适配
响应式图片
```

安全区域：

```css
padding-bottom: env(safe-area-inset-bottom);
```

---

## 4. 样式工程化

需要掌握：

```js
Sass / Less
CSS Modules
PostCSS
Tailwind CSS
CSS-in-JS
原子化 CSS
Design Token
主题切换
暗黑模式
```

主题变量：

```css
:root {
  --primary-color: #1677ff;
  --bg-color: #ffffff;
}

[data-theme='dark'] {
  --bg-color: #141414;
}
```

---

## 九、Node 与全栈能力

五年前端最好具备 Node 服务端能力，至少能写中间层、BFF、脚手架和自动化工具。

## 1. Node 基础

需要掌握：

```js
事件循环
CommonJS
ESM
fs
path
http
stream
buffer
process
child_process
cluster
worker_threads
```

Node 事件循环阶段：

```txt
timers
pending callbacks
idle / prepare
poll
check
close callbacks
```

重点区别：

```js
process.nextTick
Promise.then
setTimeout
setImmediate
```

---

## 2. Express / Koa / NestJS

需要掌握：

```js
路由
中间件
错误处理
参数校验
日志
鉴权
文件上传
接口限流
跨域
静态资源
```

Koa 中间件洋葱模型：

```js
app.use(async (ctx, next) => {
  console.log('1 start')
  await next()
  console.log('1 end')
})
```

---

## 3. BFF

BFF：Backend For Frontend。

适合场景：

```js
聚合多个后端接口
裁剪字段
适配前端页面
处理权限
接口缓存
灰度控制
服务端渲染
```

价值：

```txt
降低前端复杂度
减少多接口串联
提升页面性能
隔离后端变化
```

---

## 4. 数据库基础

前端全栈需要了解：

```js
MySQL
PostgreSQL
MongoDB
Redis
SQL 基础
索引
事务
连接池
ORM
```

常见 ORM：

```js
Prisma
TypeORM
Sequelize
Mongoose
```

---

## 5. 登录鉴权

需要掌握：

```js
Session
Cookie
JWT
OAuth2
SSO
扫码登录
Refresh Token
权限模型
RBAC
ABAC
```

JWT 注意：

```txt
不要存敏感信息
注意过期时间
注意刷新机制
服务端仍需校验权限
```

---

## 十、微前端与架构能力

五年前端在大厂或中大型项目中经常遇到微前端。

## 1. 微前端解决的问题

```txt
多个团队独立开发
老项目渐进迁移
技术栈不统一
独立部署
业务模块解耦
```

方案：

```js
qiankun
single-spa
Module Federation
iframe
Web Components
```

---

## 2. 微前端核心问题

```js
路由隔离
样式隔离
JS 沙箱
应用通信
公共依赖共享
构建部署
权限统一
状态共享
性能优化
```

样式隔离方案：

```js
CSS Modules
BEM
Shadow DOM
动态样式卸载
命名空间
```

---

## 十一、测试体系

五年前端要理解测试，而不是完全依赖人工点页面。

## 1. 单元测试

工具：

```js
Jest
Vitest
Testing Library
```

适合测试：

```js
工具函数
hooks
组件逻辑
状态变更
边界条件
```

---

## 2. 组件测试

重点：

```js
渲染是否正确
交互是否正确
事件是否触发
异步状态是否符合预期
```

---

## 3. E2E 测试

工具：

```js
Cypress
Playwright
```

适合：

```js
登录流程
核心业务链路
表单提交
支付流程
权限流程
```

---

## 十二、CI/CD 与部署

需要掌握：

```js
Git
Git Flow
GitHub Actions
GitLab CI
Jenkins
Docker
Nginx
静态资源部署
灰度发布
回滚
环境变量
构建缓存
自动化测试
```

常见流程：

```txt
提交代码
  ↓
代码检查
  ↓
单元测试
  ↓
构建
  ↓
上传产物
  ↓
部署
  ↓
通知
```

Nginx 前端部署常见配置：

```nginx
location / {
  try_files $uri $uri/ /index.html;
}
```

用于解决前端 History 路由刷新 404。

---

## 十三、监控与稳定性

五年前端需要具备线上问题排查能力。

## 1. 错误监控

需要监控：

```js
JS 运行错误
Promise 未捕获错误
资源加载失败
接口错误
白屏
框架错误边界
```

React：

```jsx
class ErrorBoundary extends React.Component {
  componentDidCatch(error, info) {
    reportError(error, info)
  }

  render() {
    return this.props.children
  }
}
```

---

## 2. 性能监控

采集：

```js
FP
FCP
LCP
CLS
INP
TTFB
接口耗时
资源加载耗时
首屏时间
```

---

## 3. 用户行为监控

常见内容：

```js
PV
UV
点击行为
页面停留
路由变化
转化漏斗
异常操作路径
```

---

## 4. sourcemap

线上错误定位需要 sourcemap。

注意：

```txt
sourcemap 不建议直接公开
可以上传到监控平台
生产环境使用 hidden-source-map
```

---

## 十四、前端设计模式与代码质量

需要掌握：

```js
单例模式
工厂模式
策略模式
观察者模式
发布订阅
代理模式
装饰器模式
适配器模式
责任链模式
组合模式
```

业务中常用：

```js
策略模式处理复杂条件
发布订阅处理事件通信
适配器处理接口数据转换
责任链处理表单校验或审批流程
工厂模式处理动态组件
```

示例：策略模式

```js
const strategies = {
  vip: price => price * 0.8,
  normal: price => price,
  student: price => price * 0.6
}

function getPrice(type, price) {
  return strategies[type](price)
}
```

---

## 十五、五年前端项目经验包装方向

面试时重点不是背知识点，而是要能结合项目讲落地。

## 1. 企业后台 / SaaS 系统

重点讲：

```js
权限系统
动态路由
动态表单
通用表格
请求封装
错误处理
性能优化
组件抽象
工程规范
```

可以这样表达：

> 我负责过企业级后台系统的前端架构建设，包括基于角色权限生成动态路由和菜单，封装通用表格、动态表单和请求层，并结合 ESLint、Prettier、Husky、CI 流程提升团队协作效率。

---

## 2. H5 / 移动端项目

重点讲：

```js
移动端适配
性能优化
弱网处理
图片优化
埋点
分享
登录态
兼容性
```

---

## 3. 大屏可视化

重点讲：

```js
ECharts
地图
WebSocket
数据轮询
自适应缩放
性能优化
动画控制
异常兜底
```

---

## 4. 低代码 / 配置化平台

重点讲：

```js
schema 设计
动态渲染
组件协议
拖拽
属性面板
表达式引擎
权限控制
版本管理
预览发布
```

---

## 5. 组件库建设

重点讲：

```js
组件 API 设计
主题系统
按需加载
文档站点
单元测试
Monorepo
npm 发布
版本管理
```

---

## 十六、五年前端面试高频问题清单

### JS

```txt
说一下原型链
闭包是什么，有什么应用和风险
this 指向规则
new 做了什么
Promise 原理
async/await 原理
事件循环输出题
ESM 和 CommonJS 区别
深拷贝怎么实现
防抖节流怎么实现
并发请求控制怎么实现
```

### React

```txt
React Diff 原理
为什么需要 key
Fiber 是什么
Hooks 为什么不能写在条件里
useEffect 和 useLayoutEffect 区别
React.memo 有什么用
如何优化 React 性能
Redux 和 Zustand 区别
React Query 解决什么问题
```

### Vue

```txt
Vue2 和 Vue3 响应式区别
Proxy 相比 defineProperty 的优势
computed 和 watch 区别
Vue nextTick 原理
Vue Diff 原理
Vue3 编译优化有哪些
Pinia 和 Vuex 区别
keep-alive 原理
```

### 工程化

```txt
Webpack 构建流程
Loader 和 Plugin 区别
Vite 为什么快
Tree Shaking 原理
Babel 原理
如何做项目规范
如何设计前端架构
如何做组件库
Monorepo 解决什么问题
```

### 浏览器与网络

```txt
输入 URL 到页面展示发生了什么
HTTP 缓存
强缓存和协商缓存
HTTP/1.1、HTTP/2、HTTP/3 区别
HTTPS 握手过程
跨域如何解决
浏览器渲染流程
重排和重绘区别
Cookie、localStorage、sessionStorage 区别
```

### 安全

```txt
XSS 是什么，如何防御
CSRF 是什么，如何防御
Cookie 的 HttpOnly、Secure、SameSite
前端如何做权限控制
JWT 有什么风险
npm 供应链安全怎么处理
```

### 性能

```txt
首屏优化怎么做
大列表怎么优化
图片怎么优化
React/Vue 项目如何排查性能问题
LCP、CLS、INP 是什么
Webpack 包体积怎么优化
如何做性能监控
```

### Node

```txt
Node 事件循环
Koa 洋葱模型
Express 和 Koa 区别
BFF 是什么
JWT 登录怎么设计
文件上传怎么处理
如何做接口限流
如何做日志和错误处理
```

---

## 十七、五年前端的能力分层

### 第一层：业务开发能力

```txt
能独立完成页面
能处理复杂表单
能封装组件
能对接接口
能处理权限
能解决常见 bug
```

### 第二层：工程建设能力

```txt
能搭项目
能做规范
能封装请求层
能配置构建工具
能优化打包
能设计目录结构
```

### 第三层：性能与稳定性能力

```txt
能分析首屏慢
能定位线上错误
能处理白屏问题
能做性能监控
能做异常上报
能优化大列表和复杂组件
```

### 第四层：架构设计能力

```txt
能设计权限系统
能设计低代码 schema
能设计组件库
能做微前端方案
能做 BFF
能拆分模块
```

### 第五层：团队协作能力

```txt
能制定规范
能做 code review
能推动技术升级
能写技术文档
能带新人
能评估技术方案
```

---

## 十八、建议学习顺序

比较合理的顺序是：

```txt
JS 深度
  ↓
浏览器与网络
  ↓
React / Vue 框架原理
  ↓
工程化与工具链
  ↓
性能优化
  ↓
安全
  ↓
Node 与全栈
  ↓
架构设计与项目包装
```

不要一开始就死磕框架源码。更好的方式是：

```txt
先理解使用场景
再理解核心原理
再看关键源码
最后结合项目输出方案
```

---

# 最后总结

五年前端工程师的核心竞争力不只是“会 React/Vue”，而是这几件事：

```txt
1. JS 底层扎实，能解释复杂运行机制
2. 熟悉 React/Vue 原理，知道框架为什么这样设计
3. 能搭建和治理中大型前端项目
4. 能解决权限、表单、表格、请求、缓存等业务复杂度
5. 理解浏览器、网络、安全和性能优化
6. 掌握工具链，能优化构建和开发体验
7. 具备 Node/BFF/全栈意识
8. 能做技术方案、推动规范、排查线上问题
```

一句话：

**五年前端的标准，不是“我能写功能”，而是“我能把复杂业务稳定、可维护、高性能地交付出来”。**
