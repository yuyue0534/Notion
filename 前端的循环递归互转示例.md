可以把这句话理解成：

> **循环和递归在“表达能力”上基本等价，差别主要在于“状态保存在哪里”。**
>
> * 循环：状态通常保存在变量里。
> * 递归：状态通常保存在**函数调用栈**里。
> * 把循环改成递归：把循环变量变成递归参数。
> * 把递归改成循环：自己用变量、数组 `stack` / `queue` 模拟调用栈。

不过在 JavaScript 的实际工程中，虽然“理论上互通”，**并不意味着两者在性能、安全性、可读性上完全等价**。

---

# 一、最简单的例子：`for` 循环 → 递归

比如输出 1~5。

### 循环写法

```js
for (let i = 1; i <= 5; i++) {
  console.log(i);
}
```

这里实际上有三个状态：

```text
初始值：i = 1
终止条件：i <= 5
状态变化：i++
```

递归只要把这三个东西搬进函数即可：

```js
function print(i) {
  if (i > 5) return;

  console.log(i);

  print(i + 1);
}

print(1);
```

执行过程相当于：

```text
print(1)
  ↓
print(2)
  ↓
print(3)
  ↓
print(4)
  ↓
print(5)
  ↓
print(6)
  ↓
return
```

所以：

```js
for (let i = 1; i <= 5; i++)
```

本质上可以理解成：

```js
function loop(i) {
  if (i > 5) return;

  // 循环体

  loop(i + 1);
}
```

这就是最基本的：

> **循环变量 → 递归参数**

---

# 二、数组循环 → 递归

平时前端最常见：

```js
const arr = ['A', 'B', 'C', 'D'];

for (let i = 0; i < arr.length; i++) {
  console.log(arr[i]);
}
```

递归版本：

```js
const arr = ['A', 'B', 'C', 'D'];

function walk(arr, index = 0) {
  if (index >= arr.length) return;

  console.log(arr[index]);

  walk(arr, index + 1);
}

walk(arr);
```

执行：

```text
walk(arr, 0)
    A

walk(arr, 1)
    B

walk(arr, 2)
    C

walk(arr, 3)
    D

walk(arr, 4)
    return
```

所以数组遍历根本不依赖 `for`。

---

# 三、`while` 循环也可以转成递归

例如：

```js
let n = 5;

while (n > 0) {
  console.log(n);
  n--;
}
```

递归：

```js
function countdown(n) {
  if (n <= 0) return;

  console.log(n);

  countdown(n - 1);
}

countdown(5);
```

输出：

```text
5
4
3
2
1
```

这里：

```js
while (条件) {
  操作
  修改状态
}
```

基本都可以转成：

```js
function loop(state) {
  if (!条件) return;

  操作;

  loop(新的state);
}
```

这实际上就是一个非常通用的转换模板。

---

# 四、循环累加 → 递归

例如求：

```text
1 + 2 + 3 + ... + 100
```

循环：

```js
let sum = 0;

for (let i = 1; i <= 100; i++) {
  sum += i;
}

console.log(sum);
```

递归可以这样：

```js
function sum(n) {
  if (n === 0) return 0;

  return n + sum(n - 1);
}

console.log(sum(100));
```

展开看看：

```js
sum(5)

= 5 + sum(4)

= 5 + 4 + sum(3)

= 5 + 4 + 3 + sum(2)

= 5 + 4 + 3 + 2 + sum(1)

= 5 + 4 + 3 + 2 + 1 + sum(0)

= 5 + 4 + 3 + 2 + 1 + 0

= 15
```

这时候就能看出递归一个非常重要的特征：

> **后面的计算结果会逐层返回给前面的调用。**

---

# 五、反过来：递归 → 循环

经典例子就是阶乘。

数学定义：

```text
5! = 5 × 4 × 3 × 2 × 1
```

递归写法：

```js
function factorial(n) {
  if (n <= 1) return 1;

  return n * factorial(n - 1);
}

console.log(factorial(5));
```

执行过程：

```text
factorial(5)
= 5 * factorial(4)

= 5 * 4 * factorial(3)

= 5 * 4 * 3 * factorial(2)

= 5 * 4 * 3 * 2 * factorial(1)

= 5 * 4 * 3 * 2 * 1

= 120
```

完全可以改成循环：

```js
function factorial(n) {
  let result = 1;

  for (let i = 2; i <= n; i++) {
    result *= i;
  }

  return result;
}
```

或者：

```js
function factorial(n) {
  let result = 1;

  while (n > 1) {
    result *= n;
    n--;
  }

  return result;
}
```

所以这种简单递归，转换成循环非常直接。

---

# 六、关键问题来了：复杂递归怎么转循环？

前面的例子都很简单，因为只有一条递归路线。

真正能体现：

> **递归和循环为什么互通**

的是树结构。

假设：

```js
const tree = {
  name: 'A',
  children: [
    {
      name: 'B',
      children: [
        { name: 'D', children: [] },
        { name: 'E', children: [] }
      ]
    },
    {
      name: 'C',
      children: [
        { name: 'F', children: [] }
      ]
    }
  ]
};
```

结构：

```text
        A
       / \
      B   C
     / \   \
    D   E   F
```

---

# 七、树遍历：递归版本

这是递归非常擅长的场景。

```js
function walk(node) {
  console.log(node.name);

  for (const child of node.children) {
    walk(child);
  }
}

walk(tree);
```

输出：

```text
A
B
D
E
C
F
```

为什么递归特别自然？

因为树本身就是递归结构：

```text
节点
 ├─ 子节点
 │   ├─ 子节点
 │   └─ 子节点
 └─ 子节点
```

也就是说：

```text
Tree =
    Node
    +
    Tree[]
```

所以：

```js
walk(node)
```

里面再次：

```js
walk(child)
```

非常符合数据结构本身。

---

# 八、树递归 → 循环：自己创建一个栈

递归之所以能工作，是因为 JavaScript 引擎在背后维护：

```text
Call Stack
调用栈
```

既然如此，我们完全可以：

> 不让 JS 帮我们维护栈，而是自己维护一个数组。

```js
function walk(root) {
  const stack = [root];

  while (stack.length > 0) {
    const node = stack.pop();

    console.log(node.name);

    for (let i = node.children.length - 1; i >= 0; i--) {
      stack.push(node.children[i]);
    }
  }
}

walk(tree);
```

结果仍然：

```text
A
B
D
E
C
F
```

这里：

```js
const stack = [root];
```

其实就是：

> **手动模拟函数调用栈。**

---

# 九、为什么这里要倒着 `push`？

注意：

```js
for (let i = node.children.length - 1; i >= 0; i--) {
  stack.push(node.children[i]);
}
```

因为 stack 是：

```text
后进先出
LIFO
```

假设：

```text
A.children = [B, C]
```

我们希望先处理：

```text
B
```

因此必须：

```js
stack.push(C);
stack.push(B);
```

这样：

```js
stack.pop()
```

首先拿到的才是：

```text
B
```

过程大概是：

```text
初始：

stack
[A]
```

拿出 A：

```text
[]
```

加入：

```text
C
B
```

于是：

```text
[C, B]
```

`pop()`：

```text
B
```

然后 B 的：

```text
E
D
```

入栈：

```text
[C, E, D]
```

下一次：

```text
pop()
```

得到：

```text
D
```

最终：

```text
A → B → D → E → C → F
```

这就是 DFS：

> Depth First Search，深度优先搜索。

---

# 十、这实际上揭示了递归的本质

例如：

```js
function walk(node) {
  console.log(node.name);

  for (const child of node.children) {
    walk(child);
  }
}
```

看起来代码里面没有：

```js
stack
```

但其实浏览器内部存在：

```text
JavaScript Call Stack
```

可以粗略理解成：

```text
walk(A)
    ↓
walk(B)
    ↓
walk(D)
```

调用 D 时：

```text
┌───────────┐
│ walk(D)   │
├───────────┤
│ walk(B)   │
├───────────┤
│ walk(A)   │
└───────────┘
```

`walk(D)` 执行结束：

```text
pop
```

恢复：

```text
walk(B)
```

然后处理 E：

```text
┌───────────┐
│ walk(E)   │
├───────────┤
│ walk(B)   │
├───────────┤
│ walk(A)   │
└───────────┘
```

所以：

> **递归其实就是把“循环状态”交给 JavaScript 调用栈保存。**

而递归转循环经常就是：

> **把隐式 Call Stack 转成显式 `stack = []`。**

这是理解两者互通最关键的一点。

---

# 十一、两个 `for` 循环，也能改成递归

例如：

```js
for (let i = 0; i < 3; i++) {
  for (let j = 0; j < 3; j++) {
    console.log(i, j);
  }
}
```

结果：

```text
0 0
0 1
0 2
1 0
1 1
1 2
2 0
2 1
2 2
```

完全递归版本：

```js
function loop(i, j) {
  if (i >= 3) return;

  console.log(i, j);

  if (j < 2) {
    loop(i, j + 1);
  } else {
    loop(i + 1, 0);
  }
}

loop(0, 0);
```

本质上只是状态：

```js
i
j
```

由：

```js
for
```

控制，变成了递归参数控制。

---

# 十二、任意层级的嵌套循环，递归反而更好写

这个例子就非常有意思。

假设我要生成：

```text
3 个位置
每个位置都有 A / B / C 三个选择
```

例如：

```text
AAA
AAB
AAC
ABA
ABB
...
CCC
```

如果固定三层，可以：

```js
const chars = ['A', 'B', 'C'];

for (const a of chars) {
  for (const b of chars) {
    for (const c of chars) {
      console.log(a + b + c);
    }
  }
}
```

但是如果需求变成：

```text
生成 100 层呢？
```

难道写：

```js
for (...)
  for (...)
    for (...)
      for (...)
        ...
```

100 个？

显然不可能。

递归：

```js
const chars = ['A', 'B', 'C'];

function generate(current, depth) {
  if (depth === 3) {
    console.log(current);
    return;
  }

  for (const char of chars) {
    generate(current + char, depth + 1);
  }
}

generate('', 0);
```

如果想变成 100 层：

```js
generate('', 100);
```

逻辑完全不用改变。

这就是：

> **动态层级问题特别适合递归。**

---

# 十三、前端开发里非常典型的递归：菜单

例如后端返回：

```js
const menus = [
  {
    name: '系统管理',
    children: [
      {
        name: '用户管理',
        children: []
      },
      {
        name: '权限管理',
        children: [
          {
            name: '角色管理',
            children: []
          }
        ]
      }
    ]
  }
];
```

你根本不知道菜单有几层：

```text
系统管理
 ├─ 用户管理
 └─ 权限管理
      └─ 角色管理
           └─ ???
```

递归处理：

```js
function printMenus(menus, level = 0) {
  for (const menu of menus) {
    console.log(
      ' '.repeat(level * 2) + menu.name
    );

    if (menu.children?.length) {
      printMenus(menu.children, level + 1);
    }
  }
}

printMenus(menus);
```

输出：

```text
系统管理
  用户管理
  权限管理
    角色管理
```

这在 Vue 里面也非常常见，例如：

```text
RecursiveMenu.vue
```

组件内部继续调用：

```vue
<RecursiveMenu
  v-for="child in item.children"
  :item="child"
/>
```

所以像：

* 无限级菜单
* 组织架构树
* 部门树
* 评论回复
* 文件夹目录
* BOM
* 权限树
* 分类树

这些结构天然适合递归。

---

# 十四、文件目录也是非常典型的递归结构

例如：

```js
const files = {
  name: 'src',
  type: 'folder',
  children: [
    {
      name: 'components',
      type: 'folder',
      children: [
        {
          name: 'Button.vue',
          type: 'file'
        }
      ]
    },
    {
      name: 'App.vue',
      type: 'file'
    }
  ]
};
```

递归：

```js
function printFile(node, level = 0) {
  console.log(
    ' '.repeat(level * 2) + node.name
  );

  if (node.type === 'folder') {
    for (const child of node.children) {
      printFile(child, level + 1);
    }
  }
}

printFile(files);
```

输出：

```text
src
  components
    Button.vue
  App.vue
```

这就是 VSCode 左侧：

```text
Explorer
```

文件树背后的典型思想之一。

---

# 十五、更复杂一点：斐波那契数列

递归：

```js
function fib(n) {
  if (n <= 1) return n;

  return fib(n - 1) + fib(n - 2);
}
```

比如：

```js
fib(5)
```

实际上形成一棵树：

```text
              fib(5)
             /      \
         fib(4)     fib(3)
         /   \       /   \
    fib(3) fib(2) fib(2) fib(1)
     ...
```

所以这里甚至不是简单的一条递归链，而是：

```text
递归树
```

依然能改成循环：

```js
function fib(n) {
  if (n <= 1) return n;

  let a = 0;
  let b = 1;

  for (let i = 2; i <= n; i++) {
    const next = a + b;

    a = b;
    b = next;
  }

  return b;
}
```

而且循环版本效率高很多。

例如：

```js
fib(40)
```

普通递归会大量重复计算：

```text
fib(38)
fib(37)
fib(36)
...
```

而循环只计算：

```text
1 → 2 → 3 → ... → 40
```

一次。

因此：

> **能互相转换 ≠ 两种写法性能一样。**

---

# 十六、递归转循环的“万能办法”：显式 Stack

假如有一个比较复杂的递归：

```js
function dfs(node) {
  process(node);

  for (const child of node.children) {
    dfs(child);
  }
}
```

转换思路基本就是：

```js
const stack = [root];

while (stack.length) {

  const node = stack.pop();

  process(node);

  // 把下一步要递归的东西放入 stack
}
```

所以可以把：

```text
递归
```

抽象成：

```text
处理当前状态
    ↓
产生新的状态
    ↓
调用自己处理新状态
```

循环版本：

```text
把初始状态放进 stack

while stack 不为空
    ↓
取出状态
    ↓
处理状态
    ↓
产生新状态
    ↓
加入 stack
```

因此很多算法：

```text
DFS
回溯
树遍历
迷宫
文件遍历
组合排列
```

都可以从：

```text
递归
```

转换为：

```text
while + stack
```

---

# 十七、反过来，循环转递归的通用思想

例如：

```js
let state = initialState;

while (condition(state)) {
  state = next(state);
}
```

基本可以写成：

```js
function loop(state) {
  if (!condition(state)) {
    return state;
  }

  return loop(next(state));
}

loop(initialState);
```

例如：

```js
let x = 1;

while (x < 100) {
  x *= 2;
}
```

改成：

```js
function loop(x) {
  if (x >= 100) {
    return x;
  }

  return loop(x * 2);
}

console.log(loop(1));
```

执行：

```text
1
2
4
8
16
32
64
128
```

---

# 十八、所以可以建立一个非常重要的对应关系

可以把两者这样对应理解：

| 循环                 | 递归                    |
| ------------------ | --------------------- |
| `for / while`      | 函数调用自己                |
| 循环变量               | 函数参数                  |
| `while(condition)` | 递归终止条件                |
| `i++`              | `fn(i + 1)`           |
| 局部变量保存状态           | 调用栈保存状态               |
| `break`            | `return`              |
| `continue`         | 直接进入下一次递归             |
| 手动 `stack`         | JavaScript Call Stack |

尤其记住这一组：

```text
循环：
状态在变量里

递归：
状态在调用栈里
```

---

# 十九、为什么递归会 Stack Overflow，而循环通常不会？

例如：

```js
function test(n) {
  console.log(n);
  test(n + 1);
}

test(1);
```

最终会报：

```text
Maximum call stack size exceeded
```

因为：

```text
test(1)
  test(2)
    test(3)
      test(4)
        test(5)
          ...
```

每调用一次：

```text
Call Stack + 1
```

最后把 JavaScript 调用栈撑爆。

而：

```js
let i = 1;

while (true) {
  i++;
}
```

虽然这是死循环，但它通常不会因为：

```text
Call Stack
```

溢出。

因为始终只处于同一个函数调用层级。

当然它会把 CPU 占满、卡死页面，这是另一回事。

---

# 二十、这也是实际前端开发中不能“全部用递归”的原因

理论上：

```js
for (let i = 0; i < 100000; i++) {
  // ...
}
```

可以改成：

```js
function loop(i) {
  if (i >= 100000) return;

  loop(i + 1);
}

loop(0);
```

但是 JavaScript 很可能直接：

```text
RangeError:
Maximum call stack size exceeded
```

所以虽然：

> 数学/计算理论意义上循环可以转换为递归。

工程上：

> **大量简单重复通常优先循环。**

---

# 二十一、尾递归是一个特殊概念

例如：

```js
function sum(n, total = 0) {
  if (n === 0) {
    return total;
  }

  return sum(n - 1, total + n);
}
```

这里：

```js
return sum(...)
```

是函数最后一步。

称为：

```text
Tail Recursion
尾递归
```

理论上 JavaScript 引擎可以优化：

```text
不用保留上一层 stack frame
```

于是：

```text
递归
```

几乎能够做到类似循环的内存消耗。

但实际开发不能依赖这一点，因为主流 JavaScript 引擎对尾调用优化的支持并不一致。

所以不要认为：

```js
return fn(...)
```

就一定不会栈溢出。

---

# 二十二、还有一种技巧：Trampoline

如果确实想保留递归风格，又不想疯狂增长调用栈，可以把：

```js
function fn() {
  return fn();
}
```

变成：

```js
function fn() {
  return () => fn();
}
```

然后用循环执行：

```js
function trampoline(fn) {
  while (typeof fn === 'function') {
    fn = fn();
  }

  return fn;
}
```

例如：

```js
function sum(n, total = 0) {
  if (n === 0) {
    return total;
  }

  return () => sum(n - 1, total + n);
}

console.log(
  trampoline(() => sum(100000))
);
```

本质上还是：

> **把递归交给循环驱动。**

所以 trampoline 其实又一次证明：

```text
递归 ↔ 循环
```

确实可以互相转换。

---

# 二十三、还有一个很值得理解的区别：递归有“进入”和“返回”

例如：

```js
function test(n) {
  if (n === 0) return;

  console.log('进入', n);

  test(n - 1);

  console.log('返回', n);
}

test(3);
```

输出：

```text
进入 3
进入 2
进入 1

返回 1
返回 2
返回 3
```

为什么？

调用过程：

```text
test(3)
    ↓
test(2)
    ↓
test(1)
    ↓
test(0)
```

然后开始返回：

```text
test(0)
    ↑
test(1)
    ↑
test(2)
    ↑
test(3)
```

所以递归天然存在两个阶段：

```text
向下递归
↓
↓
↓

达到终点

↑
↑
↑
逐层回溯
```

这就是很多：

```text
树遍历
回溯算法
组合排列
迷宫
路径搜索
```

特别喜欢递归的原因。

---

# 二十四、如果改成循环，就必须自己保存这些“回来以后做什么”的信息

比如递归：

```js
function test(n) {
  if (n === 0) return;

  console.log('进入', n);

  test(n - 1);

  console.log('返回', n);
}
```

JavaScript 自动帮你记住：

```text
test(3) 还没完成
test(2) 还没完成
test(1) 还没完成
```

等 `test(0)` 完成以后继续。

循环版本就要自己保存：

```js
function test(n) {
  const stack = [];

  while (n > 0) {
    console.log('进入', n);

    stack.push(n);

    n--;
  }

  while (stack.length) {
    console.log('返回', stack.pop());
  }
}
```

输出一样：

```text
进入 3
进入 2
进入 1
返回 1
返回 2
返回 3
```

这里的：

```js
stack.push(n);
```

其实就在模拟：

```text
JavaScript Call Stack
```

---

# 二十五、所以“所有递归都可以变循环”真正难的地方在哪里？

不是：

```text
能不能变
```

而是：

```text
变完以后代码会不会非常难看。
```

例如递归：

```js
function walk(node) {
  for (const child of node.children) {
    walk(child);
  }
}
```

非常简单。

如果里面存在：

```js
处理前
递归A
处理A结果
递归B
处理B结果
回退状态
```

改循环以后通常需要保存：

```js
{
  node,
  index,
  state,
  phase,
  result
}
```

例如 stack 里面不再只是：

```js
stack.push(node);
```

而可能变成：

```js
stack.push({
  node,
  childIndex: 0,
  phase: 'before',
  parentResult: result
});
```

本质就是：

> 原本 JavaScript Call Stack 自动保存的东西，现在全部由程序员自己保存。

所以有时候递归版 20 行：

```text
↓
```

循环版可能变成 50～100 行。

---

# 二十六、什么时候应该用循环？

前端 JavaScript 中，下面这些通常优先循环。

普通数组遍历：

```js
for (const item of list) {
}
```

大量数据：

```js
for (let i = 0; i < 100000; i++) {
}
```

简单累加：

```js
let total = 0;

for (const item of list) {
  total += item.amount;
}
```

分页处理：

```js
while (hasMore) {
  // 获取下一页
}
```

固定次数操作：

```js
for (let i = 0; i < 10; i++) {
}
```

因为：

```text
简单
性能稳定
不会产生深层调用栈
容易调试
```

---

# 二十七、什么时候递归明显更舒服？

碰到：

```text
数据本身就是嵌套结构
```

通常优先考虑递归。

典型：

```text
树
目录
菜单
部门
评论回复
DOM
AST
JSON
权限树
组件树
分类树
组织架构
```

比如：

```js
function findNode(node, id) {
  if (node.id === id) {
    return node;
  }

  for (const child of node.children ?? []) {
    const result = findNode(child, id);

    if (result) {
      return result;
    }
  }

  return null;
}
```

这种递归写法非常自然。

---

# 二十八、前端还有一个非常典型的例子：深度处理 JSON

例如：

```js
const data = {
  user: {
    name: 'Yuri',
    settings: {
      theme: 'dark',
      language: 'zh'
    }
  }
};
```

想遍历所有属性：

```js
function walk(obj) {
  for (const [key, value] of Object.entries(obj)) {
    console.log(key, value);

    if (
      value &&
      typeof value === 'object'
    ) {
      walk(value);
    }
  }
}

walk(data);
```

它可以处理：

```text
1 层
5 层
10 层
100 层
```

代码都不用改。

如果纯循环写，则需要自己维护：

```js
const stack = [data];
```

例如：

```js
function walk(data) {
  const stack = [data];

  while (stack.length) {
    const obj = stack.pop();

    for (const [key, value] of Object.entries(obj)) {
      console.log(key, value);

      if (
        value &&
        typeof value === 'object'
      ) {
        stack.push(value);
      }
    }
  }
}
```

再次体现：

```text
递归
    ↓

JS Call Stack
```

和：

```text
循环
    ↓

自己的 stack[]
```

本质非常接近。

---

# 二十九、还有一个常被忽略的东西：队列 Queue

并不是所有递归转循环都只使用：

```js
stack
```

如果需要：

```text
深度优先 DFS
```

一般：

```js
stack
```

如果需要：

```text
广度优先 BFS
```

一般：

```js
queue
```

例如：

```js
function bfs(root) {
  const queue = [root];

  while (queue.length) {
    const node = queue.shift();

    console.log(node.name);

    for (const child of node.children) {
      queue.push(child);
    }
  }
}
```

对于：

```text
        A
       / \
      B   C
     / \   \
    D   E   F
```

输出：

```text
A
B
C
D
E
F
```

所以真正更高一层的理解不是：

```text
for vs recursion
```

而是：

```text
状态如何组织？
```

可能是：

```text
变量
Stack
Queue
Call Stack
```

---

# 三十、可以把程序执行抽象成“状态机”

这也是我认为理解这句话最有价值的一层。

比如：

```js
for (let i = 0; i < 10; i++) {
  console.log(i);
}
```

其实程序状态就是：

```text
state = i
```

每次：

```text
i = i + 1
```

所以：

```text
state0
 ↓
state1
 ↓
state2
 ↓
state3
 ↓
...
```

递归：

```js
function loop(i) {
  if (i >= 10) return;

  console.log(i);

  loop(i + 1);
}
```

实际上仍然：

```text
state0
 ↓
state1
 ↓
state2
 ↓
...
```

因此从更抽象的角度：

> **循环和递归只是描述“状态不断转换”的两种不同语法形式。**

---

# 三十一、一个非常实用的记忆方法

以后看到循环：

```js
let state = init;

while (condition) {

  doSomething();

  state = nextState;
}
```

你可以脑补：

```js
function recursive(state) {

  if (!condition) {
    return;
  }

  doSomething();

  recursive(nextState);
}
```

而看到：

```js
function recursive(state) {

  if (结束条件) return;

  process(state);

  recursive(nextState);
}
```

你可以脑补：

```js
let state = initialState;

while (!结束条件) {

  process(state);

  state = nextState;
}
```

如果递归比较复杂：

```js
recursive(A)
recursive(B)
recursive(C)
```

那么：

```text
↓
```

通常就考虑：

```js
const stack = [];
```

自己模拟调用栈。

---

# 三十二、最后把这句话稍微修正一下，会更加严谨

你说：

> 所有的循环都能转成递归，所有的递归都能转成循环，它们之间是互通的。

从**计算能力和算法表达**角度，这个认识基本正确。

但工程上最好理解为：

> **循环和递归具有等价的表达能力；循环可以通过递归调用表达，递归也可以通过循环 + 显式状态栈表达。**

但是：

```text
表达能力相同
        ≠
代码一样简单
        ≠
性能一样
        ≠
内存消耗一样
        ≠
实际开发中可以随便替换
```

实际开发经验可以粗略概括成：

```text
简单、固定、重复
    ↓
循环

嵌套、树形、层级未知
    ↓
递归

递归层级非常深
    ↓
循环 + stack

需要逐层回退状态
    ↓
递归通常更直观

数据量特别大
    ↓
优先避免深递归
```

而其中最值得真正理解的一句话就是：

> **递归不是没有循环，它只是把“下一次循环”和“历史状态”交给了函数调用栈。**
>
> **循环也不是不能递归，它只是把递归调用栈展开成了显式的变量、`stack` 或 `queue`。**

一旦把 **Call Stack（调用栈）** 这一层理解透，递归、DFS、回溯、树遍历、AST、DOM 树、组织架构树这些前端常见问题基本就会串成一套知识体系。
