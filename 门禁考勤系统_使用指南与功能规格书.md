# 门禁 / 考勤系统 —— 使用指南与功能规格书

> 本文档基于系统前端源码整理，涵盖角色权限、各页面功能规格、审批流程详解。
> 适用读者：普通员工、审批人（直属上级 / 上级主管）、系统管理员。

---

## 目录

- [一、系统概述](#一系统概述)
- [二、角色与权限](#二角色与权限)
- [三、页面导航一览](#三页面导航一览)
- [四、各角色使用指南](#四各角色使用指南)
- [五、审批流程详解（重点）](#五审批流程详解重点)
- [六、功能规格书（分模块）](#六功能规格书分模块)
- [七、已知限制与注意事项](#七已知限制与注意事项)

---

## 一、系统概述

本系统是一套门禁打卡 + 考勤统计 + 请假 / 考勤异常说明审批一体化的内部管理系统，主要解决三类问题：

1. **原始打卡数据的查询与统计**：门禁设备上传的原始刷卡记录，经过规则计算后生成"每日考勤明细""周度汇总""打卡分析"等多维度视图。
2. **考勤异常的申诉流程**：员工对"迟到 / 早退 / 缺卡 / 工时不足 / 缺勤"等异常状态，可以在线提交说明并走审批。
3. **请假申请与审批**：员工可在线提交请假申请（年假 / 病假 / 事假等），走审批流程。

系统登录邮箱限定为 `@pccw.com` 域名，账号与"员工主数据"中的工号（Staff ID）绑定后才能使用完整功能。

---

## 二、角色与权限

系统里实际存在的"角色"不是写死的枚举，而是由两个字段动态决定的：

| 角色 | 判定依据 | 说明 |
|---|---|---|
| **普通员工** | 已登录、`profiles.is_admin = false` | 默认角色，只能看自己相关的数据和申请 |
| **审批人（直属上级 / 上级主管）** | 别人的 `employees.report_manager` 指向了我 | 不是单独身份，是"员工主数据"里给某人配置了审批人之后，被指向的人自动获得对应申请的审批权限 |
| **管理员** | `profiles.is_admin = true` | 拥有全部数据查看权限 + 员工主数据维护 + 规则设置 + 审批兜底 |

**权限速查表**（✅=可见/可用，❌=不可见，🔒=仅管理员）：

| 功能 | 普通员工 | 被设为审批人的员工 | 管理员 |
|---|:---:|:---:|:---:|
| 原始打卡记录 / 每日明细 / 全员矩阵 / 周度汇总 | ✅（全公司数据，非仅自己） | ✅ | ✅ |
| 发起我的申请（考勤说明 / 请假） | ✅ | ✅ | ✅ |
| 审批别人的申请 | 仅当自己被设为对方审批人时可见对应工单 | ✅ | ✅（兜底审批全部转 admin 阶段的工单） |
| 审批历史查询 | ✅（可查看全部历史，非仅自己经手的） | ✅ | ✅ |
| 员工主数据维护（增删改、组织架构） | ❌ | ❌ | 🔒 |
| 规则设置（考勤规则 / 每日明细默认过滤规则） | ❌ | ❌ | 🔒 |

> ⚠️ 注意：原始打卡记录、每日明细、全员矩阵、周度汇总这几个查询类页面**目前没有做"仅本人可见"的限制**，任何登录用户都能查询全公司的打卡与考勤数据，权限模型主要卡在"写操作"（审批、员工主数据维护、规则设置）上，而不是"查询范围"上。如果这不符合预期，需要和管理员确认是否要补充行级权限控制。

---

## 三、页面导航一览

| 侧边栏名称 | 路径 | 说明 | 可见性 |
|---|---|---|---|
| 原始打卡记录 | `/` | 门禁设备上传的刷卡流水，支持导出 Excel | 全员 |
| 每日明细 | `/daily` | 按人 + 日汇总的考勤结果（含异常状态），支持导出、查看当日原始打卡明细、发起异常说明 | 全员 |
| 全员矩阵 | `/matrix` | 横向对比多人 × 多日的考勤状态一览表 | 全员 |
| 周度汇总 | `/weekly` | 按人 + 自然周汇总的出勤天数 / 工时统计 | 全员 |
| 我的申请 | `/my-requests` | 发起 / 查看自己提交的考勤说明与请假申请 | 全员 |
| 待我审批 | `/approval-inbox` | 当前分配到自己名下、待处理的审批工单 | 全员（若无工单则为空列表） |
| 审批历史 | `/approval-history` | 全部审批动作的流水记录，可导出 | 全员 |
| 规则设置 | `/settings` | 考勤计算规则 + 每日明细默认过滤规则 | 🔒 管理员 |
| 员工主数据 | `/employees` | 员工信息增删改、组织架构查看 | 🔒 管理员 |

以下两个页面**当前未挂载到侧边栏导航**，需要直接输入网址访问：

| 页面 | 路径 | 说明 |
|---|---|---|
| 仪表盘 | `/dashboard` | 单日全员出勤快照（KPI + 甜甜圈图）+ 近 10 个工作日出勤正常率趋势 |
| 打卡分析 | `/analysis` | 迟到 / 早退 / 缺卡 / 工时不足的统计分析 + 上班打卡时间分布直方图 |

---

## 四、各角色使用指南

### 4.1 普通员工

**日常查看考勤**
1. 打开「每日明细」，用日期范围 + 姓名 / 工号筛选，找到自己某一天的考勤结果。
2. 点击某一行的「查看明细」，可以看到当天参与计算的全部原始打卡记录，以及净工时的计算过程说明。
3. 如果某天状态显示异常（迟到 / 早退 / 缺卡异常 / 工时不足 / 缺勤），该行会出现「发起说明」按钮。

**对异常发起说明**
1. 在「每日明细」页点击某一异常行的「发起说明」，会自动跳转到「我的申请」页并预填好日期和异常类型。
2. 也可以直接进入「我的申请」页，手动选择「考勤异常说明」类型，填写日期、勾选异常类型（可多选）、填写说明原因，按需上传附件（最多 10 个，单个不超过 10MB），提交。
3. 提交后可以在页面下方「我的申请记录」里看到工单状态（审批中 / 已通过 / 已驳回），审批中或草稿状态可以点击「撤回」。
4. 「每日明细」页对应行会同步显示一个"说明"审批状态小标签，方便一眼看出这条异常有没有在处理中。

**请假**
1. 在「我的申请」页切换到「请假」标签页，选择请假类型、日期范围（支持选同一天并勾选"按半天请假"），填写请假原因，可选上传附件，提交。
2. 请假天数按自然日计算（**暂不排除周末和法定节假日**，提交前请自行核对天数是否符合预期）。

**如果自己恰好是别人的审批人**
- 见下一节 4.2，操作方式完全一样，「待我审批」页会自动出现。

### 4.2 被设为审批人的员工（直属上级 / 上级主管）

如果在「员工主数据」里，某个员工的"审批人"字段指向了你，那么该员工提交的申请流转到你这一级时，会出现在你的「待我审批」页面，同时侧边栏"待我审批"菜单项会出现未处理数量的红色角标。

1. 打开「待我审批」，逐条查看申请人、类型、日期、摘要、说明原因、附件。
2. 点击附件文件名可以在线预览（图片 / PDF 支持内嵌预览，其他格式需下载查看）。
3. 在「备注」输入框填写审批意见（**驳回时必填，通过时选填**），点击「通过」或「驳回」，会弹出二次确认框。
4. 操作成功后，工单会流转到下一审批阶段（或结束流程），角标数字实时更新。

### 4.3 管理员

管理员除了拥有普通员工和审批人的全部能力外，额外负责：

**员工主数据维护**（「员工主数据」页）
- 新增 / 编辑 / 删除员工，维护姓名、中文名、Entity、Team Lead（自由文本，用于组织架构展示）、员工类型（固定员工 / 长期出差 / 临时出差）、在职状态（在职 / 停用 / 离职）、审批人（下拉选择，驱动审批流路由）。
- 「组织架构」按钮可查看按 Team Lead 分组的树形 / 文本两种视图。
- ⚠️ 删除员工前请确认该员工没有关联的在途申请 / 登录账号，否则会因为数据库外键约束删除失败。

**规则设置**（「规则设置」页）
- **固定班次与弹性规则**：最晚上班时间、最早下班时间、午休扣除小时数、最低净工时——这四个值决定了全公司"迟到 / 早退 / 工时不足"的判定标准，保存后**所有页面立即按新规则重新计算**，请谨慎修改。
- **每日明细默认过滤规则**：勾选要在「每日明细」页面和导出 Excel 中排除的"在职状态"（停用 / 离职）和"员工类型"，不影响打卡分析 / 全员矩阵 / 仪表盘。

**审批兜底**
- 凡是流转到 `admin` 阶段的工单，任意管理员都能在「待我审批」里看到并处理，不要求该工单的 `current_approver_staff_id` 精确指向自己。

---

## 五、审批流程详解（重点）

### 5.1 两种申请类型

| 业务类型 | 说明 | 关键字段 |
|---|---|---|
| `attendance_explanation`（考勤异常说明） | 针对某一天的异常（迟到/早退/缺卡/工时不足/缺勤）做说明 | 日期、异常类型（可多选）、说明原因 |
| `leave`（请假） | 请假申请 | 请假类型、开始/结束日期、是否半天、说明原因 |

两种类型共用同一套审批工单表结构（`approval_requests`），只是明细数据分别存在 `attendance_explanation_details` / `leave_details` 两张表里，前端通过 `business_type` 字段区分并加载对应详情。

### 5.2 审批阶段

系统里能观察到的审批阶段有三级：

| 阶段代码 | 中文名 | 说明 |
|---|---|---|
| `leader` | 直属上级 | 通常对应申请人在「员工主数据」里设置的"审批人"（`report_manager`） |
| `manager` | 上级主管 | 更上一级的审批 |
| `admin` | 管理员 | 兜底阶段，任意管理员均可处理 |

> 📌 **需要说明的一点**：审批工单具体"从哪个阶段开始、经过几级才能结束"这套路由规则，是在数据库函数（`create_attendance_explanation_request` / `create_leave_request` / `act_on_approval_request`）内部实现的，**前端代码里看不到具体判断逻辑**。从可观察到的现象推断：
> - 「员工主数据」页的提示文案写明——"不设置审批人的话，该员工发起的申请会直接转给管理员审批"，说明 `report_manager` 为空时工单会跳过 `leader`/`manager` 阶段直接进入 `admin`。
> - 「待我审批」的查询条件是 `current_approver_staff_id = 我的工号` **或者**（我是管理员 **且** `current_stage = 'admin'`），说明只有 `admin` 阶段是"任意管理员都能接"，其余阶段必须精确匹配到具体某个人。
>
> 如果需要 100% 准确的阶段流转规则（例如 `leader` 通过后一定会进入 `manager` 吗？`manager` 是否也是可选的？），需要查看数据库里 `act_on_approval_request` 这个函数的具体实现，或向负责数据库的同事确认。

### 5.3 工单状态

| 状态代码 | 中文名 | 说明 |
|---|---|---|
| `draft` | 草稿 | 理论状态，当前前端所有提交入口都是"填完直接提交"，不经过草稿态 |
| `pending` | 审批中 | 已提交，等待当前阶段审批人处理 |
| `approved` | 已通过 | 全部审批阶段完成 |
| `rejected` | 已驳回 | 任意一级审批人驳回，流程终止 |
| `withdrawn` | 已撤回 | 申请人在 `draft` 或 `pending` 状态下主动撤回 |

`draft` / `pending` 状态下，申请人可以在「我的申请」页点击「撤回」；其余状态不可撤回。

### 5.4 完整生命周期（申请人视角）

```
┌──────────────┐   提交    ┌──────────┐   逐级审批通过   ┌──────────┐
│ 填写申请表单  │ ───────→ │  pending │ ──────────────→ │ approved │
│（附件可选）   │           └────┬─────┘                  └──────────┘
└──────────────┘                │
                                 │ 任意一级驳回（须填备注）
                                 ▼
                            ┌──────────┐
                            │ rejected │
                            └──────────┘

  pending 状态下申请人可随时点击"撤回" ──→ withdrawn
```

### 5.5 附件规则

- 单个文件不超过 **10MB**，单个工单最多 **10 个附件**。
- 支持在线预览的类型：图片（`image/*`）、PDF；其他类型只能下载查看。
- 附件的签名下载链接**有效期 5 分钟**，超时需要重新点击预览。
- 申请人在工单处于可撤回状态（`draft`/`pending`）时可以删除自己上传的附件；审批人只能查看/下载，不能删除。

### 5.6 通知机制

- 侧边栏「待我审批」菜单项会显示待处理工单数量的红色角标（超过 99 显示"99+"）。
- 角标数据来源于每 60 秒一次的轮询 + 路由切换 / 身份变化时的即时刷新 + 审批操作成功后的即时刷新，**不是数据库实时推送**，极端情况下（比如别人刚提交、你恰好停留在同一个页面）最多有 60 秒的延迟。

### 5.7 数据导出

- 「审批历史」页支持按当前筛选条件导出 Excel（审批时间 / 动作 / 审批人 / 申请人 / 类型 / 日期 / 摘要 / 备注），单次最多导出 **5000 条**，超过会在导出结果提示里注明"已达上限"。

---

## 六、功能规格书（分模块）

### 6.1 原始打卡记录（`/`）

| 项目 | 说明 |
|---|---|
| 数据源 | `access_records` 表（门禁设备原始上传数据） |
| 筛选条件 | 日期范围（含快捷预设：今天/昨天/本周/上周/本月）、员工姓名（模糊）、员工编号（模糊） |
| 列表字段 | 时间、设备名称、事件点、事件描述、卡号、员工编号、姓名、部门、进/出、验证方式 |
| 分页 | 默认每页 50 条，可切换 50/100/200 |
| 导出 | 按当前筛选条件导出全部数据（不受分页限制），文件名含日期范围 |
| 特殊处理 | 部分历史数据存在中文乱码（UTF-8 字节被误按 Windows-1252 解码），前端统一做了自动修复 |

### 6.2 每日明细（`/daily`）

| 项目 | 说明 |
|---|---|
| 数据源 | RPC 函数 `get_full_daily_attendance`（在职员工 × 日期范围笛卡尔积，无打卡记录的工作日也会生成"缺勤"行） |
| 筛选条件 | 日期范围、姓名、工号、考勤状态下拉（正常/周末/缺勤/缺卡异常/迟到/早退/工时不足及其组合） |
| 列表字段 | 日期、姓名、员工编号、部门、最早打卡、最晚打卡、打卡次数、工时、考勤状态、审批状态（异常说明工单的旁注标签）、操作 |
| 操作 | 「查看明细」弹出当天原始打卡记录抽屉；异常行显示「发起说明」跳转到「我的申请」并预填条件 |
| 导出 | Excel 三个 / **升级后四个** sheet（见 6.2.1），按当前日期范围 + 姓名/工号筛选，**不含状态下拉筛选**（因为各 sheet 有各自固定口径） |

#### 6.2.1 导出 Excel 详细规格（v2，含"非FS Team"拆分）

| Sheet 顺序 | Sheet 名 | 数据范围 |
|---|---|---|
| 1 | FS Team员工每日考勤异常明细 | 员工类型 = 固定员工（`FIXED`）的异常记录，仅工作日（周一~周五） |
| 2 | 非FS Team员工每日考勤异常明细 | 员工类型 ≠ 固定员工（长期出差 `LONG_TERM` / 临时出差 `SHORT_TERM` 等）的异常记录，仅工作日 |
| 3 | 全体员工每日考勤明细 | 全部记录（含正常/周末），不区分员工类型 |
| 4 | 员工周度考勤汇总 | 按人 + 自然周聚合，周度出勤总天数固定按 5 天口径计算 |

字段（Sheet 1/2/3 通用）：员工编号、员工姓名、日期、星期、最早打卡时间、最晚打卡时间、当日总时长、当日打卡次数、考勤状态、Team Lead。

字段（Sheet 4）：员工编号、员工姓名、统计周期、周度出勤总天数、正常出勤天数、异常考勤天数、周度总工时、平均日工时、Team Lead。

> ⚠️ 平均日工时固定按"周度总工时 ÷ 5"计算，**如果导出的日期范围不是完整的自然周（比如只选了 2、3 天），这个数字会明显失真**，请优先选择完整周范围导出周度汇总数据。

### 6.3 全员矩阵（`/matrix`）

| 项目 | 说明 |
|---|---|
| 数据源 | `daily_attendance` 视图，一次性拉取（非分页），单次上限 5000 行 |
| 筛选条件 | 日期范围（最长不超过 31 天）、姓名、工号 |
| 展示形式 | 纵轴为人（姓名/工号/部门），横轴为日期，每个格子显示当天状态（正常/迟到等组合/缺卡/无记录） |

### 6.4 周度汇总（`/weekly`）

| 项目 | 说明 |
|---|---|
| 数据源 | `weekly_summary` 视图，标准分页查询 |
| 筛选条件 | 周范围（按 `week_start` 过滤）、姓名、工号 |
| 列表字段 | 周范围、姓名、工号、部门、出勤天数、缺卡天数、迟到天数、早退天数、工时不足天数、总工时、日均工时 |
| 备注 | 本页筛选条件不接入全局记忆（切换页面再回来会重置），与其余几个查询页略有差异 |

### 6.5 打卡分析（`/analysis`）— 未挂载到导航

| 项目 | 说明 |
|---|---|
| 数据源 | `daily_attendance` 视图，一次性拉取，单次上限 5000 行 |
| 内容 | 迟到/早退/缺卡/工时不足人次及占比、平均迟到时长、上班打卡时间分布直方图（按小时）、异常明细列表（最多显示 200 条） |
| 特殊说明 | 页面顶部会实时读取当前生效的考勤规则文案（不写死数字） |

### 6.6 仪表盘（`/dashboard`）— 未挂载到导航

| 项目 | 说明 |
|---|---|
| 数据源 | `daily_attendance` 视图，按单日快照 + 近 21 个自然日回溯取最近 10 个"有数据的工作日" |
| 内容 | 5 张 KPI 卡片、当日状态分布甜甜圈图、近 10 个工作日出勤正常率趋势条形图、"需要关注"明细表（异常人员） |
| 分类口径 | 每人按"缺卡 > 迟到 > 早退 > 工时不足 > 正常"优先级只归入一个分类，**与「每日明细」「打卡分析」允许一人多种异常并存的口径不同**，两处数字对比时请留意这个差异 |

### 6.7 我的申请（`/my-requests`）

见 [5. 审批流程详解](#五审批流程详解重点)，此处补充字段规格：

| 表单字段（考勤说明） | 说明 |
|---|---|
| 日期 | 必填 |
| 异常类型 | 必填，可多选：迟到/早退/工时不足/缺卡异常/缺勤 |
| 说明原因 | 必填，最多 500 字 |

| 表单字段（请假） | 说明 |
|---|---|
| 请假类型 | 年假/病假/事假/婚假/丧假/产假/陪产假/其他 |
| 请假日期范围 | 必填，不能早于今天 |
| 半天请假 | 仅同一天可勾选，勾选后天数固定为 0.5 |
| 请假原因 | 必填，最多 500 字 |

列表字段：类型、日期、摘要、说明、附件、状态、当前节点、提交时间、操作（撤回）。

### 6.8 待我审批（`/approval-inbox`）

列表字段：申请人、类型、日期、摘要、说明、附件、当前节点、提交时间、备注（输入框）、操作（通过/驳回）。

### 6.9 审批历史（`/approval-history`）

| 项目 | 说明 |
|---|---|
| 筛选条件 | 日期范围、申请人（姓名/工号模糊）、申请类型、"只看我处理的"（仅管理员可见此勾选项） |
| 列表字段 | 审批时间、动作（通过/驳回）、审批人、审批环节、申请人、申请类型、日期、摘要、说明、备注、附件 |
| 导出 | 见 5.7 |

### 6.10 规则设置（`/settings`，管理员）

**固定班次与弹性规则**

| 字段 | 默认值 | 说明 |
|---|---|---|
| 最晚上班时间 | 09:30 | 首次有效进入晚于此时间标记"迟到" |
| 最早下班时间 | 18:30 | 最后有效离开早于此时间标记"早退" |
| 午休扣除（小时） | 1.5 | 净工时计算时扣除 |
| 最低净工时（小时） | 8 | 净工时低于此值标记"工时不足" |

净工时公式：`最晚打卡 − 最早打卡 − 午休扣除`。

**每日明细默认过滤规则**

| 字段 | 说明 |
|---|---|
| 排除在职状态 | 可勾选"停用""离职" |
| 排除员工类型 | 可勾选"固定员工""长期出差""临时出差" |

> 仅影响「每日明细」页面和其 Excel 导出，不影响打卡分析 / 全员矩阵 / 仪表盘。

### 6.11 员工主数据（`/employees`，管理员）

| 字段 | 说明 |
|---|---|
| 员工编号 | 主键，创建后不可修改 |
| 员工姓名 / 中文姓名 | 姓名必填 |
| Entity | 自由文本 |
| Team Lead | 自由文本，仅用于组织架构可视化展示，**不驱动审批流** |
| 员工类型 | 固定员工 `FIXED` / 长期出差 `LONG_TERM` / 临时出差 `SHORT_TERM` |
| 审批人 | 下拉选择在职员工，驱动审批流路由（对应数据库 `report_manager` 字段），不设置则直接转管理员审批 |
| 在职状态 | 在职 / 停用 / 离职 |

**组织架构视图**：按 Team Lead 文本递归建树（支持多层级：一个 Team Lead 自己也可能是别人的下属），提供树形视图（可展开/收起）和纯文本视图（方便整段复制）两种展示方式。

> ⚠️ 组织架构树是按"姓名文本匹配"搭建的，与驱动审批流的"审批人"字段是两套独立数据，仅用于可视化展示，不要混淆。

### 6.12 登录 / 注册（`/login`）

- 仅允许 `@pccw.com` 邮箱注册，密码至少 6 位。
- 注册时需填写员工编号（Staff ID），用于关联「员工主数据」，**当前流程不核验填写人身份是否与该工号真实对应**，请勿把工号透露给他人代注册。
- 支持"忘记密码"邮件重置流程。
- 会话策略：空闲 **10 分钟**自动登出（到期前 2 分钟弹窗提醒），最长会话 **8 小时**（无论是否操作，到点强制退出重新登录）。

---

## 七、已知限制与注意事项

1. **原始打卡记录 / 每日明细 / 全员矩阵 / 周度汇总为全公司数据**，非仅本人数据，普通员工也能查询他人考勤，请知悉。
2. **仪表盘、打卡分析两个页面暂未挂载到侧边栏导航**，需直接访问 `/dashboard`、`/analysis`。
3. **审批阶段的具体流转规则（几级、如何跳级）实现在数据库函数内**，本文档只能基于前端可观察行为推断，如需精确规则请与后端 / DBA 确认。
4. **周度汇总导出**的"出勤总天数"和"平均日工时"按固定 5 天口径计算，非完整自然周的筛选范围下数据会失真。
5. **仪表盘的异常分类**与「每日明细」「打卡分析」口径不同（互斥单分类 vs 允许多异常并存），跨页面比较数字时请留意。
6. **请假天数**按自然日计算，不排除周末和法定节假日。
7. **组织架构树**基于 Team Lead 姓名文本匹配搭建，与实际驱动审批流的"审批人"字段无关，仅供可视化参考。
8. **注册流程不核验员工身份真实性**，请通过内部渠道妥善保管自己的工号信息。

---

*文档生成基于系统当前代码版本整理，如页面或流程有更新，请以实际系统为准。*
