## 一、数据处理

### 遍历 / 排序 / 去重 / 查找 / 分组 / 聚合

```html
<!DOCTYPE html>
<html lang="zh">
<head>
<meta charset="UTF-8">
<title>数据处理完整案例</title>
</head>
<body>
<script>
/* ─── 1. 遍历 ─── */
const arr = [3, 1, 4, 1, 5, 9, 2, 6, 5, 3];

arr.forEach((n, i) => console.log(i, n));              // 纯遍历
const doubled = arr.map(n => n * 2);                   // 映射
const evens   = arr.filter(n => n % 2 === 0);          // 过滤
const sum      = arr.reduce((acc, n) => acc + n, 0);   // 累积

/* ─── 2. 多字段稳定排序 ─── */
// 先按 age 升序，age 相同按 name 字典序
const people = [
  { name: 'Charlie', age: 25 },
  { name: 'Alice',   age: 30 },
  { name: 'Bob',     age: 25 },
];
const sorted = [...people].sort((a, b) =>
  a.age - b.age || a.name.localeCompare(b.name)
);
// 结果: Bob(25) Charlie(25) Alice(30)

/* ─── 3. 去重 ─── */
// 基础类型
const unique1 = [...new Set([1, 2, 2, 3, 3])];       // [1,2,3]

// 对象按 id 去重（保留后出现的）
const list = [{ id:1, v:'a' }, { id:2, v:'b' }, { id:1, v:'c' }];
const unique2 = [...new Map(list.map(x => [x.id, x])).values()];

// 多字段组合去重
const unique3 = list.filter((x, i, arr) =>
  arr.findIndex(y => y.city === x.city && y.type === x.type) === i
);

/* ─── 4. 查找 ─── */
const found     = arr.find(n => n > 5);            // 第一个 >5 的值
const foundIdx  = arr.findIndex(n => n > 5);       // 对应下标

// 二分查找（要求数组已排序）
function binarySearch(arr, target) {
  let lo = 0, hi = arr.length - 1;
  while (lo <= hi) {
    const mid = (lo + hi) >> 1;
    if (arr[mid] === target) return mid;
    arr[mid] < target ? (lo = mid + 1) : (hi = mid - 1);
  }
  return -1;
}

/* ─── 5. 分组 groupBy ─── */
// 原生 Object.groupBy (ES2024) 或 polyfill
function groupBy(arr, keyFn) {
  return arr.reduce((acc, item) => {
    const key = keyFn(item);
    (acc[key] ??= []).push(item);
    return acc;
  }, {});
}
const products = [
  { name: 'Apple',  category: 'fruit', price: 3 },
  { name: 'Carrot', category: 'veg',   price: 2 },
  { name: 'Mango',  category: 'fruit', price: 5 },
];
const byCategory = groupBy(products, p => p.category);
// { fruit: [{...},{...}], veg: [{...}] }

/* ─── 6. 聚合 ─── */
function aggregate(arr, numKey) {
  const vals = arr.map(x => x[numKey]);
  const sum   = vals.reduce((a, b) => a + b, 0);
  return {
    sum,
    avg:   +(sum / vals.length).toFixed(2),
    min:   Math.min(...vals),
    max:   Math.max(...vals),
    count: vals.length,
  };
}
console.log(aggregate(products, 'price'));
// { sum:10, avg:3.33, min:2, max:5, count:3 }
</script>
</body>
</html>

<!-- ===使用说明===
- 遍历：map/filter/reduce 不改变原数组，forEach 常用于副作用操作
- 排序：sort 会改变原数组，先 spread 复制再排。多字段用 || 链式比较
- 去重：基础类型用 Set；对象按主键用 Map；多字段组合用 findIndex
- 查找：find/findIndex 是 O(n)；数据量大时先排序再用二分 O(logn)
- 分组：reduce 实现最通用，也可用 ES2024 原生 Object.groupBy
- 聚合：先 map 取目标字段，再做统计，保持职责分离
-->
```

---

## 二、结构处理

### 树遍历 / 扁平化 / 图依赖

```html
<!DOCTYPE html>
<html lang="zh">
<head><meta charset="UTF-8"><title>结构处理完整案例</title></head>
<body>
<script>
const tree = {
  id: 1, name: 'root', children: [
    { id: 2, name: 'A', children: [
        { id: 4, name: 'A1', children: [] },
        { id: 5, name: 'A2', children: [] },
    ]},
    { id: 3, name: 'B', children: [
        { id: 6, name: 'B1', children: [] },
    ]},
  ]
};

/* ─── 1. DFS 深度优先（递归 / 迭代） ─── */
// 递归版
function dfsRecursive(node, result = []) {
  result.push(node.id);
  (node.children || []).forEach(c => dfsRecursive(c, result));
  return result;  // [1,2,4,5,3,6]
}

// 迭代版（栈）—— 无调用栈溢出风险
function dfsIterative(root) {
  const stack = [root], result = [];
  while (stack.length) {
    const node = stack.pop();
    result.push(node.id);
    // 逆序入栈保证左先右后
    [...(node.children || [])].reverse().forEach(c => stack.push(c));
  }
  return result;
}

/* ─── 2. BFS 广度优先（队列） ─── */
function bfs(root) {
  const queue = [root], result = [];
  while (queue.length) {
    const node = queue.shift();
    result.push(node.id);
    (node.children || []).forEach(c => queue.push(c));
  }
  return result;  // [1,2,3,4,5,6]
}

/* ─── 3. 树 → 扁平列表（带 pid） ─── */
function treeToFlat(node, pid = null, result = []) {
  result.push({ id: node.id, name: node.name, pid });
  (node.children || []).forEach(c => treeToFlat(c, node.id, result));
  return result;
}

/* ─── 4. 扁平列表 → 树（O(n) Map 方案） ─── */
function flatToTree(list) {
  const map = Object.fromEntries(
    list.map(n => [n.id, { ...n, children: [] }])
  );
  let root;
  list.forEach(n => {
    if (n.pid != null) map[n.pid].children.push(map[n.id]);
    else root = map[n.id];
  });
  return root;
}

/* ─── 5. 数组递归扁平化（兼容旧环境） ─── */
function flatDeep(arr, depth = Infinity) {
  return depth > 0
    ? arr.reduce((acc, val) =>
        acc.concat(Array.isArray(val) ? flatDeep(val, depth - 1) : val), [])
    : arr.slice();
}
// 现代环境直接用: [1,[2,[3]]].flat(Infinity)

/* ─── 6. 图依赖拓扑排序（Kahn 算法） ─── */
// graph: { 节点: [依赖它的节点] } 即邻接表（有向边 A→B 表示 B 依赖 A）
function topoSort(graph) {
  // 计算入度
  const inDegree = {};
  Object.keys(graph).forEach(n => { inDegree[n] ??= 0; });
  Object.values(graph).flat().forEach(n => {
    inDegree[n] = (inDegree[n] || 0) + 1;
  });

  const queue  = Object.keys(inDegree).filter(n => inDegree[n] === 0);
  const result = [];

  while (queue.length) {
    const node = queue.shift();
    result.push(node);
    (graph[node] || []).forEach(dep => {
      if (--inDegree[dep] === 0) queue.push(dep);
    });
  }

  const allNodes = Object.keys(inDegree);
  if (result.length !== allNodes.length) {
    const cycle = allNodes.filter(n => !result.includes(n));
    throw new Error(`检测到循环依赖: ${cycle.join(' ↔ ')}`);
  }
  return result;
}

// 示例：webpack chunk 依赖
const deps = {
  'utils':   ['core'],
  'core':    ['app'],
  'lodash':  ['app', 'core'],
  'app':     [],
};
try {
  console.log(topoSort(deps)); // ['utils','lodash','core','app'] 类似顺序
} catch (e) {
  console.error(e.message);
}
</script>
</body>
</html>

<!-- ===使用说明===
- DFS 递归：代码简洁，树深超过 ~10000 层有栈溢出风险
- DFS 迭代：用显式栈替代调用栈，可安全处理超深树
- BFS：需要层序遍历（如 DOM 操作、最短路径）时选择
- treeToFlat：数据库存储场景，将树转为带 pid 的记录
- flatToTree：前端从接口拿到列表数据，重建展示用树
- 拓扑排序：构建系统、打包工具、任务调度等依赖解析场景
  - 边方向约定要统一：本例 A→B 表示"A 执行完才能执行 B"
  - 检测循环依赖：result.length < 总节点数 即存在环
-->
```

---

## 三、交互控频

### 防抖 / 节流 / 竞态 / 重试 / 轮询

```html
<!DOCTYPE html>
<html lang="zh">
<head><meta charset="UTF-8"><title>交互控频完整案例</title></head>
<body>

<!-- 防抖搜索框 -->
<input id="search" placeholder="输入关键字搜索...">

<script>
/* ─── 1. 防抖 debounce ─── */
// 场景：搜索框、表单校验、窗口 resize
function debounce(fn, delay, immediate = false) {
  let timer;
  return function (...args) {
    const callNow = immediate && !timer;
    clearTimeout(timer);
    timer = setTimeout(() => {
      timer = null;
      if (!immediate) fn.apply(this, args);
    }, delay);
    if (callNow) fn.apply(this, args);
  };
}

document.getElementById('search').addEventListener(
  'input',
  debounce(e => console.log('搜索:', e.target.value), 500)
);

/* ─── 2. 节流 throttle（时间戳版） ─── */
// 场景：scroll、mousemove、拖拽、resize
function throttle(fn, limit) {
  let lastTime = 0;
  return function (...args) {
    const now = Date.now();
    if (now - lastTime >= limit) {
      lastTime = now;
      fn.apply(this, args);
    }
  };
}

// 节流（定时器版）—— 保证最后一次一定触发
function throttleTimer(fn, limit) {
  let timer;
  return function (...args) {
    if (!timer) {
      timer = setTimeout(() => {
        timer = null;
        fn.apply(this, args);
      }, limit);
    }
  };
}

window.addEventListener('scroll', throttle(() => {
  console.log('滚动位置:', window.scrollY);
}, 200));

/* ─── 3. 请求竞态处理 ─── */

// 方案 A：requestId（不依赖 AbortController）
let currentRequestId = 0;
async function fetchWithRaceGuard(keyword) {
  const id = ++currentRequestId;
  const data = await fakeRequest(keyword);
  if (id !== currentRequestId) return; // 已被更新请求覆盖
  renderResult(data);
}

// 方案 B：AbortController（现代浏览器推荐）
let abortController = null;
async function fetchWithAbort(keyword) {
  abortController?.abort();                          // 取消上一个请求
  abortController = new AbortController();
  try {
    const res  = await fetch(`/api/search?q=${keyword}`, {
      signal: abortController.signal,
    });
    const data = await res.json();
    renderResult(data);
  } catch (err) {
    if (err.name === 'AbortError') return;           // 主动取消，静默忽略
    throw err;
  }
}

function renderResult(data) { console.log('渲染结果:', data); }
// mock
function fakeRequest(kw) {
  const delay = Math.random() * 600 + 200;
  return new Promise(r => setTimeout(() => r({ kw, delay: ~~delay }), delay));
}

/* ─── 4. 重试 retry with 指数退避 ─── */
// 场景：网络不稳定的 API 调用、上传、支付回调
async function retry(fn, options = {}) {
  const { maxAttempts = 3, baseDelay = 300, factor = 2, shouldRetry } = options;

  for (let attempt = 0; attempt < maxAttempts; attempt++) {
    try {
      return await fn(attempt);
    } catch (err) {
      const isLast = attempt === maxAttempts - 1;
      if (isLast || (shouldRetry && !shouldRetry(err))) throw err;

      const delay = baseDelay * factor ** attempt;   // 300ms, 600ms, 1200ms...
      console.warn(`第 ${attempt + 1} 次失败，${delay}ms 后重试...`);
      await sleep(delay);
    }
  }
}

function sleep(ms) { return new Promise(r => setTimeout(r, ms)); }

// 使用示例
async function uploadFile(file) {
  return retry(
    async (attempt) => {
      console.log(`上传尝试 #${attempt + 1}`);
      const res = await fetch('/api/upload', { method: 'POST', body: file });
      if (!res.ok) throw new Error(`HTTP ${res.status}`);
      return res.json();
    },
    {
      maxAttempts: 3,
      baseDelay: 500,
      // 只对网络错误重试，4xx 客户端错误不重试
      shouldRetry: err => !err.message.startsWith('HTTP 4'),
    }
  );
}

/* ─── 5. 轮询 polling（支持暂停/恢复/页面可见性） ─── */
class Poller {
  constructor(fn, interval = 3000) {
    this.fn        = fn;
    this.interval  = interval;
    this.timer     = null;
    this.running   = false;
    // 页面隐藏时暂停，回到前台时恢复
    document.addEventListener('visibilitychange', () => {
      if (this.running) {
        document.hidden ? this._pause() : this._resume();
      }
    });
  }

  start() {
    if (this.running) return;
    this.running = true;
    this._tick();
  }

  stop() {
    this.running = false;
    this._pause();
  }

  _tick() {
    this.fn();
    this.timer = setInterval(() => this.fn(), this.interval);
  }

  _pause()  { clearInterval(this.timer); }
  _resume() { this._tick(); }
}

// 使用示例：轮询订单状态
const orderPoller = new Poller(async () => {
  const status = await fetch('/api/order/123/status').then(r => r.json());
  console.log('订单状态:', status);
  if (status === 'completed') orderPoller.stop();
}, 3000);

orderPoller.start();
// 页面切到后台自动暂停，回来自动继续
// 订单完成后自动停止
</script>
</body>
</html>

<!-- ===使用说明===

防抖 vs 节流 选择：
  - 防抖：只关心"最后那次"结果 → 搜索框、表单验证、按钮防重复提交
  - 节流：关心"持续过程中的采样" → scroll、mousemove、拖拽、游戏帧

竞态处理选择：
  - requestId：简单场景，fetch 无法取消时（如 XMLHttpRequest 旧代码）
  - AbortController：现代推荐，真正取消网络请求，节省带宽

重试注意：
  - 4xx 状态码（参数错误、未授权）不要重试，否则浪费资源
  - 加 jitter（随机抖动）避免多客户端同时重试造成"惊群"：
    delay = baseDelay * factor ** attempt + Math.random() * 100

轮询注意：
  - 一定要在组件卸载时调用 poller.stop()
  - visibilitychange 处理可避免标签页在后台持续耗电/占用带宽
  - 若后端支持 Server-Sent Events 或 WebSocket，优先用推送替代轮询
-->
```

---

**三个 Tab 对应三大类，交互 Demo 可直接操作验证逻辑，HTML 文件可直接复制运行。** 核心要点总结：

| 场景 | 核心技巧 | 注意事项 |
|---|---|---|
| 排序 | `...spread` 再排，多字段 `\|\|` 链 | sort 会改原数组 |
| 去重 | 对象用 `Map` by key | Set 对对象无效 |
| 树遍历 | 超深树用迭代栈 | 避免递归栈溢出 |
| 扁平/重建 | `flatToTree` 用 `Map` | O(n²) 的 find 慎用 |
| 防抖 | `clearTimeout` 覆盖 | 区分 leading/trailing |
| 竞态 | `AbortController` | 静默 AbortError |
| 重试 | 指数退避 + shouldRetry | 4xx 不重试 |
| 轮询 | `visibilitychange` 暂停 | 卸载时务必 stop |
